import { FC } from 'react';

interface NewAccountProps {
}

const NewAccountPage: FC<NewAccountProps> = ( {} ) => {
    return (
        <div>NewAccount Page</div>
    );
};

export default NewAccountPage;
